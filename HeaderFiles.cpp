#include<iostream>
//Header files are of two types:- system defined and user defined. compiler already have system defined but user has
//to define the header file in same directory. in this i have used this.h as user defined header file.
#include "this.h"
using namespace std;
int main(){
    cout<<"this is header file topic";
    return 0;
}